#include <stdio.h>

/**
 *main - beginning
 *Description: task 3 of 0x01
 *Return: 0
 */
int main(void)
{
char low = 'a';
while (low <= 'z')
{
putchar(low);
low++;
}
char up = 'A';
while (up <= 'Z')
{
putchar(up);
up++;
}
putchar('\n');
return (0);
}
