#include <stdio.h>
/**
 * main - beginning.
 * @argc: number of argumenys.
 * @argv: array that contains the program arguments.
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
